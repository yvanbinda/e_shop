import 'package:flutter/material.dart';
import 'package:flutter_svg/svg.dart';

class SvgFormat extends StatelessWidget {
  const SvgFormat({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}
